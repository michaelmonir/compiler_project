#include <gtest/gtest.h>
#include "dfa_convertor.h"

// Define a test case using Google Test
TEST(TRY_DFA_CONVERTOR, two) {
    // Test that try_gtest_func1() returns 5
    EXPECT_EQ(4, 5);  // This checks if the function returns 5
}
